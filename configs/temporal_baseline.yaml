model:
  kind: temporal_cnn
  measurement_aware: true
  hidden_channels: 64
  kernel_size: 5
  dropout: 0.15

training:
  seed: 20260904
  epochs: 30
  batch_size: 128
  learning_rate: 0.001
  weight_decay: 0.0001
  early_stopping_patience: 5

evaluation:
  confidence_threshold: 0.70
  calibration_bins: 10
