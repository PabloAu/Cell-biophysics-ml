# Notebooks

Notebooks are reserved for documented exploration and figures. Reusable logic
belongs in `src/`, and every reportable notebook should run from a clean kernel
against a versioned dataset manifest.
